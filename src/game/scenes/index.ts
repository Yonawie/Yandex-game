/**
 * Optional future split: Menu / Play / Shop / Result as Phaser scenes.
 * Today phases live in Game.ts and MainScene bridges Canvas frame.
 * Keep names for YANDEX_STACK folder contract.
 */
export { MainScene as MenuScene } from "./MainScene";
export { MainScene as PlayScene } from "./MainScene";
export { MainScene as ResultScene } from "./MainScene";
