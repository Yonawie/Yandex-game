{
  "name": "echo",
  "private": true,
  "version": "2.0.0",
  "description": "Эхо — ломай стену букв своим словом для Яндекс Игр",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "play": "node scripts/play.mjs",
    "pack": "npm run build && node scripts/pack-yandex.mjs",
    "typecheck": "tsc --noEmit",
    "atlas": "node scripts/atlas.mjs",
    "store:shots": "node scripts/capture-store.mjs"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "playwright": "^1.51.1",
    "pngjs": "^7.0.0",
    "sharp": "^0.35.3",
    "typescript": "^5.7.3",
    "vite": "^6.0.11"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
