{
  "name": "naydi-chto-to",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Найди что-то — поиск предметов для Яндекс Игр (TypeScript + Vite + Phaser 3)",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "start": "vite --host 0.0.0.0 --port 4173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "ok": "node scripts/validate.mjs",
    "pack": "npm run build && node scripts/pack.mjs",
    "tunnel": "bash scripts/preview-tunnel.sh"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
